import { useState } from "react";
import "./App.css";

// Configure to import/export alphbetically
// Generate grid from BOUNDARIES
// Generate snake on START
// Move snake around grid

function App() {
  const [snake, setSnake] = useState({ length: 1 });

  return <div></div>;
}

export default App;
