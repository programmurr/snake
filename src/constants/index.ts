const START = { x: 24, y: 23 };

const BOUNDARIES = { x: 49, y: 49 };

const DIRECTIONS = {
  up: "UP",
  down: "DOWN",
  left: "LEFT",
  right: "RIGHT",
} as const;

export { START, BOUNDARIES, DIRECTIONS };
